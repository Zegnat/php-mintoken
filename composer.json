{
    "name": "zegnat/mintoken",
    "description": "A minimal IndieAuth compatible token endpoint.",
    "type": "project",
    "version": "1.1.0",
    "license": "0BSD",
    "authors": [
        {
            "name": "Martijn van der Ven",
            "email": "martijn@vanderven.se"
        }
    ],
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.2"
    },
    "scripts": {
        "check-style": "phpcs -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 endpoint.php"
    }
}
